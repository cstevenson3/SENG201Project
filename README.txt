Dependencies: commons-io-2.6.jar

Import into eclipse as an existing eclipse project, .project and .classpath are included
and should link commons-io-2.6.jar automatically, if not add it in the java build path
in project properties.

In gameProperties.properties change "useGUI" to false for the CLI version or true for
the GUI version of the game